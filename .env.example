# Compose reads .env from the repository root; a key is optional.
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_BASE_URL=https://api.openai.com/v1
# Host path mounted read-only at /data in the backend container.
DATA_DIR=./backend/data/synthetic
BACKEND_PORT=8000
FRONTEND_PORT=5173
